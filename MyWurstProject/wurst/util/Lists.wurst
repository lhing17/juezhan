package Lists

import HashList

public class Lists
    static function newHashList(vararg int numbers) returns HashList<int>
        let list = new HashList<int>()
        for number in numbers
            list.add(number)
        return list