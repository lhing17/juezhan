---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by G_Seinfeld.
--- DateTime: 2018/11/14 13:49
---

require 'test.test-main'
local trigger_util = require 'jass.util.trigger_util'
local test_util = require 'test.util.test_util'
local timer = require 'jass.type.timer'


-- @Tested
local function test_damage_test()
    local h = test_util.player1_select_hero()
    local it = test_util.create_item(1227899447)
    et.player(1).hero.handle.hero = true
    trigger_util.trig_player_unit_event(et.player(1).handle, jass.EVENT_PLAYER_UNIT_PICKUP_ITEM, h.handle, { manipulated_item = it })

    for i = 1, 10 do
        math.randomseed(tostring(os.time()):reverse():sub(1, 6))
        local damage = math.random(50, 150)
        local tab = {
            event_damage = damage,
            event_damage_source = h.handle
        }
        trigger_util.trig_unit_event(gg_unit_N00I_0116.handle, jass.EVENT_UNIT_DAMAGED, tab)
    end
    timer.update(10)

end

test_damage_test()