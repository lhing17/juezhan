---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by G_Seinfeld.
--- DateTime: 2018/11/5 13:37
---

local gamespeed = {}

local mt = {}
gamespeed.__index = mt

mt.type = 'gamespeed'
mt.name = ''

function gamespeed.init()
    local gamespeed_names = {
        'MAP_SPEED_SLOWEST'
        'MAP_SPEED_SLOW'
        'MAP_SPEED_NORMAL'
        'MAP_SPEED_FAST'
        'MAP_SPEED_FASTEST'
    }
    for i = 1, #gamespeed_names do
        local gs = {}
        gs.name = gamespeed_names[i]
        setmetatable(gs, gamespeed)
        table.insert(gamespeed, gs)
    end
end

return gamespeed