---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by G_Seinfeld.
--- DateTime: 2018/11/06 11:47
---

local mapflag = {}

local mt = {}
mapflag.__index = mt

mt.type = 'mapflag'
mt.name = ''

function mapflag.init()
	local mapflag_names={}
	for i = 1, #mapflag_names do
		local ma = {}
		ma.name = mapflag_names[i]
		setmetatable(ma, mapflag)
		table.insert(mapflag, ma)
	end
end

return mapflag