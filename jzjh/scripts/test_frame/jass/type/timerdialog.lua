---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by G_Seinfeld.
--- DateTime: 2018/11/18 0018 9:54
---

local common_util = require 'jass.util.common_util'
local timerdialog = {}
timerdialog.all_timerdialogs = {}

local mt = {}
timerdialog.__index = mt
mt.type = 'timerdialog'
mt.title = ''
mt.title_color = {128, 128, 128, 128}
mt.time_color = {128, 128, 128, 128}
mt.speed = 1
mt.shown = false

function mt:set_title(title)
    self.title = title
end

function mt:set_title_color(r, g, b, a)
    self.title_color = {r, g, b, a}
end

function mt:set_time_color(r, g, b, a)
    self.time_color = {r, g, b, a}
end

function mt:set_speed(speed)
    self.speed = speed
end

function mt:set_time_remaining(remaining)
    self.time_remaining = remaining
end

function mt:show(flag)
    self.shown = flag
end

function mt:is_displayed()
    return self.shown
end

function mt:destroy()
    timerdialog.all_timerdialogs[self.handle_id] = nil
end

function timerdialog.create(t)
    local td = setmetatable({}, timerdialog)
    td.handle_id = common_util.generate_handle_id()
    td.timer = t
    td.time_remaining = t.remained
    timerdialog.all_timerdialogs[td.handle_id] = td
    return td

end

return timerdialog